# Canonical local PostgreSQL integration-test database.
# Change only the port in your private .env.test when a machine uses a
# non-default PostgreSQL port.
TEST_DATABASE_URL=postgresql+psycopg://alera_test_user:alera_test@localhost:5432/alera_test
